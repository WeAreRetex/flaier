Prefer the current workspace toolchain.

- Use `pnpm install` for dependency installation.
- Use `vp run <task>` from the repo root when invoking workspace tasks.
- Use `pnpm <script>` directly inside `apps/*` and `packages/*`.
- Use standard Node.js APIs and avoid Bun-specific runtime APIs.
